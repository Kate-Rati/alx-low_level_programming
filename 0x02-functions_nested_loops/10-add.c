#include "main.h"
#include <stdio.h>
/**
 * adds and returns the sum of params
 * @aint tpe number 
 * @b int type number
 * returns 0
 */
int main(void)
{
	int n;
	int a ;
	int b; 
        int sum(a + b);
        n = sum( a, b);
	printf("%d\n",n);
        return(0);
}
